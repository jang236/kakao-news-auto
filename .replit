run = "pip install -r requirements.txt && python -m uvicorn main:app --host 0.0.0.0 --port 8000"
entrypoint = "main.py"

[deployment]
run = ["sh", "-c", "pip install -r requirements.txt && python -m uvicorn main:app --host 0.0.0.0 --port 8000"]
